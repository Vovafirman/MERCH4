import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Stone Island Clone - Premium Fashion Store',
  description: 'Discover the latest collection of premium fashion items. Shop the newest arrivals and exclusive pieces.',
  keywords: 'fashion, clothing, premium, stone island, luxury',
  authors: [{ name: 'Stone Island Clone' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
} 